package com.gymsystem.entity;

public enum CoachSessionType {
    MORNING,        // morning session only
    EVENING,        // evening session only
    BOTH            // morning + evening sessions
}
